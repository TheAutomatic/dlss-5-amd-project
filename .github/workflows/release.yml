# Build OptiScaler AMD pre-SR and attach the user zip to a GitHub Release.
#
# Source default is 3 NR slots (AmdSlots); AMD_SINGLESLOT only moves it to 1.
# Ordinary Release is the shipping configuration — do not add diagnostics.
#
# Does NOT package NVIDIA DLLs, NR weights, or the author's pass DLL.
#
# Dependencies (FFX / XeSS signed bins) come from the git submodules under
# external/. `submodules: recursive` below is what puts them in the workspace —
# they are *.dll, so they are deliberately not committed directly.
#
# Link inputs (freetype.lib, FSR2/FSR31 static libs, vulkan-1.lib, detours.lib)
# live under OptiScaler/library and external/freetype. They are tracked in git
# the same way upstream OptiScaler does (git add -f despite *.lib ignore).
#
# Runner: windows-latest. The benchmarked binaries were built with the v145
# toolset (VS 2026), so we pin PLATFORM_TOOLSET to match rather than letting the
# project's v143 fallback pick a different compiler than the one we benchmarked.
# The preflight step fails loudly if the image no longer ships v145.

name: release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Product version, e.g. 1.9.0.3'
        required: false
        default: '1.9.0.3'

permissions:
  contents: write

env:
  PLATFORM_TOOLSET: v145
  # The MSVC toolchain the measured binaries were built with. The preflight picks
  # the newest installed 14.44.x and the build pins VCToolsVersion to it, so CI
  # produces the same compiler as the local build instead of whatever the image
  # happens to default to.
  MSVC_PREFIX: '14.44'
  BUILD_OUT: exports/release-ci

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Resolve version
        id: ver
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $v = '1.9.0.3'
          if ('${{ github.ref_type }}' -eq 'tag') {
            $v = '${{ github.ref_name }}' -replace '^v', ''
          } elseif ('${{ github.event.inputs.version }}') {
            $v = '${{ github.event.inputs.version }}'
          }
          if ($v -notmatch '^[0-9A-Za-z._-]+$') { throw "Refusing odd version string: $v" }
          "version=$v" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          Write-Host "resolved version: $v"

      - name: Toolchain preflight
        id: toolchain
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          $vs = & $vswhere -latest -products * -property installationPath
          Write-Host "Visual Studio: $vs"

          # Do NOT probe ...\MSBuild\Microsoft\VC\v*\...\PlatformToolsets by name.
          # That tree exists once per VS generation, each holds a different subset,
          # and picking one shows a misleading list: on the VS2026 image the first
          # match lists only v142 (pulled in for UWP), which reads as "v145 absent"
          # when it is in fact installed. MSVC's own directory is unambiguous.
          $msvcRoot = Join-Path $vs 'VC\Tools\MSVC'
          if (!(Test-Path $msvcRoot)) { throw "No MSVC toolchain directory at $msvcRoot" }
          $have = @(Get-ChildItem -LiteralPath $msvcRoot -Directory |
            Select-Object -ExpandProperty Name | Sort-Object)
          Write-Host "MSVC toolchains: $($have -join ', ')"
          # 14.44 is the toolchain the benchmarked binaries were built with.
          $pick = $have | Where-Object { $_ -like "$env:MSVC_PREFIX.*" } | Select-Object -Last 1
          if (-not $pick) {
            throw "No MSVC $env:MSVC_PREFIX.x toolchain on this image (have: $($have -join ', '))."
          }
          Write-Host "Using MSVC $pick (PlatformToolset=$env:PLATFORM_TOOLSET)"
          "msvc=$pick" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

          $deps = @(
            'OptiScaler-DLSSNR-PreSR-Multipass-main/external/xess/bin/libxess.dll',
            'OptiScaler-DLSSNR-PreSR-Multipass-main/external/FidelityFX-SDK-v2/Kits/FidelityFX/signedbin/amd_fidelityfx_upscaler_dx12.dll'
          )
          foreach ($d in $deps) {
            if (!(Test-Path $d)) { throw "Submodule content missing: $d (did checkout fetch submodules?)" }
          }
          Write-Host 'Submodule dependencies present.'

      - name: Link-library preflight
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          # Same as upstream OptiScaler: these *.lib are tracked despite
          # .gitignore (git add -f). They are not in the FFX/XeSS submodules.
          $libRoot = 'OptiScaler-DLSSNR-PreSR-Multipass-main/OptiScaler/library'
          $required = @(
            "$libRoot/fsr2/ffx_fsr2_api_x64.lib",
            "$libRoot/vulkan/vulkan-1.lib",
            'OptiScaler-DLSSNR-PreSR-Multipass-main/external/freetype/freetype.lib'
          )
          $missing = @($required | Where-Object { !(Test-Path $_) })
          if ($missing.Count -gt 0) {
            $missing | ForEach-Object { Write-Host "MISSING $_" }
            throw 'Link libraries missing from checkout. They must be in git (git add -f).'
          }
          Write-Host 'Link libraries present.'

      - name: MSVC dev environment
        # setup-msbuild only puts MSBuild on PATH — it deliberately does NOT add
        # cl.exe. This action runs vcvarsall so `cl` below resolves.
        # Maintained fork of ilammy/msvc-dev-cmd; the upstream one is still on
        # node20 and GitHub force-runs it on node24 with a deprecation warning.
        # Every action below is pinned to a node24 release for the same reason.
        uses: TheMrMilchmann/setup-msvc-dev@v4.1.0
        with:
          arch: x64

      - name: Add MSBuild to PATH
        uses: microsoft/setup-msbuild@v3

      - name: Installer and uninstaller regression
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          # Isolated synthetic files only; exercise the Windows PowerShell 5.1 launchers.
          python -B tests/amd_installer_exit.py
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File tests/amd_uninstall.ps1
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: AMD host-contract regression
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          # Uses a generated test PE, never a proprietary author runtime or a GPU.
          cmd /d /c tools\test-amd-host-contracts.cmd "$env:BUILD_OUT"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Build LmxxfNrRuntime DLL
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          cmd /d /c tools\build-lmxxf-runtime.cmd exports/lmxxf-runtime
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          if (!(Test-Path "exports/lmxxf-runtime/LmxxfNrRuntime.dll")) {
            throw "Build did not produce exports/lmxxf-runtime/LmxxfNrRuntime.dll"
          }

      - name: ABI regression test for LmxxfNrRuntime
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          cl /nologo /std:c++17 /O2 /EHsc /I "OptiScaler-DLSSNR-PreSR-Multipass-main/OptiScaler/dlssnr/backend/lmxxf_runtime" `
            tests/lmxxf_nr_abi.cpp /Fe:exports/lmxxf-runtime/lmxxf_nr_abi.exe
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          & exports/lmxxf-runtime/lmxxf_nr_abi.exe exports/lmxxf-runtime/LmxxfNrRuntime.dll third_party/lmxxf/modules
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          # C host smoke test: export table, capabilities, create-flag accept/reject (no GPU).
          cl /nologo /TC /W4 /I "OptiScaler-DLSSNR-PreSR-Multipass-main/OptiScaler/dlssnr/backend/lmxxf_runtime" `
            tests/lmxxf_zero_fallback_abi.c /Fo:exports/lmxxf-runtime/ /Fe:exports/lmxxf-runtime/lmxxf_zero_fallback_abi.exe
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          & exports/lmxxf-runtime/lmxxf_zero_fallback_abi.exe
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Build OptiScaler Release (default = multi-slot)
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          # Trailing *forward* slash on SolutionDir/OutDir/IntDir is required:
          # the project concatenates $(SolutionDir)external\... with no extra
          # separator, and a trailing backslash inside /p:Value="...\" makes
          # MSBuild swallow the closing quote. OutDir without a slash also
          # collapsed obj + filename (OptiScaler.pch).
          $slnDir = "$PWD/OptiScaler-DLSSNR-PreSR-Multipass-main/"
          $outDir = "$PWD/$env:BUILD_OUT/"
          # PreBuildEvent runs: it generates resource_build_{date,commit}.h, which
          # resource.h includes in Release and which are NOT in git.
          # PostBuildEvent stays off: it copies a README this fork does not have.
          msbuild "$slnDir/OptiScaler/OptiScaler.vcxproj" `
            /m:4 /t:Build /p:Configuration=Release /p:Platform=x64 `
            /p:PlatformToolset=$env:PLATFORM_TOOLSET `
            /p:VCToolsVersion=${{ steps.toolchain.outputs.msvc }} `
            /p:PostBuildEventUseInBuild=false `
            /p:SolutionDir="$slnDir" `
            /p:OutDir="$outDir" `
            /p:IntDir="${outDir}obj/"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          if (!(Test-Path "$env:BUILD_OUT/OptiScaler.dll")) {
            throw "Build did not produce $env:BUILD_OUT/OptiScaler.dll"
          }

      - name: Package user zip
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $ver = '${{ steps.ver.outputs.version }}'
          powershell -ExecutionPolicy Bypass -File tools\PACKAGE_RELEASE.ps1 `
            -Version $ver `
            -OptiDll "$env:BUILD_OUT\OptiScaler.dll" `
            -OutDir dist
          if (!(Test-Path "dist\OptiScaler-AMD-PreSR-$ver.zip")) {
            throw "Package zip missing for $ver"
          }

      - name: Guard against proprietary files
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          # Independent CI tripwire: inspect the exact archive that Release uploads.
          $ver = '${{ steps.ver.outputs.version }}'
          $zipPath = "dist\OptiScaler-AMD-PreSR-$ver.zip"
          $forbidden = '(?i)^(nvngx.*\.dll|dlssnr_amd_pass.*\.dll|dlssnr_on_amd_weights\.bin|version\.dll|dlssnr_on_amd_setup\.exe)$'
          Add-Type -AssemblyName System.IO.Compression.FileSystem
          $archive = [IO.Compression.ZipFile]::OpenRead((Resolve-Path -LiteralPath $zipPath))
          try {
            $bad = @($archive.Entries |
              Where-Object { -not [string]::IsNullOrEmpty($_.Name) -and $_.Name -match $forbidden } |
              ForEach-Object { $_.FullName })
            $entryNames = @($archive.Entries | ForEach-Object { $_.Name })
          } finally {
            $archive.Dispose()
          }
          if ($bad.Count -gt 0) {
            $bad | ForEach-Object { Write-Host "FORBIDDEN: $_" }
            throw 'Refusing to publish: proprietary/user-supplied binaries found in package zip.'
          }
          $required = @('OptiScaler.dll', 'LmxxfNrRuntime.dll', 'OptiScaler.ini', 'Setup.ps1', 'Setup.bat')
          foreach ($r in $required) {
            if ($entryNames -notcontains $r) {
              throw "REQUIRED COMPONENT MISSING IN ZIP: $r"
            }
          }

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: OptiScaler-AMD-PreSR
          path: |
            dist/OptiScaler-AMD-PreSR-${{ steps.ver.outputs.version }}.zip
            dist/OptiScaler-AMD-PreSR-${{ steps.ver.outputs.version }}/SHA256SUMS.txt
          if-no-files-found: error

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v3
        with:
          # Pre-release when tag name has an -alpha/-beta/-rc suffix (e.g. v1.9.1-alpha).
          # Formal tags like v1.9.1 stay latest/stable; plain substrings such as "source" do not count.
          prerelease: ${{ contains(github.ref_name, '-alpha') || contains(github.ref_name, '-beta') || contains(github.ref_name, '-rc') }}
          files: |
            dist/OptiScaler-AMD-PreSR-${{ steps.ver.outputs.version }}.zip
          generate_release_notes: true
          body: |
            alpha版本尚未经过实机游戏测试

            OptiScaler AMD pre-SR **${{ steps.ver.outputs.version }}**.

            ### English
            - **New `lmxxf` HIP Neural Rendering Backend:** Introduces the open-source `lmxxf` backend alongside the existing `danielblnc` backend, with improved DLSS/XeSS proxy integration.
              - **Unreal Engine 5 (UE5) Compatibility Fixes:** Resolves session invalidation and fallback issues in complex UE5 titles (tested on *Neverness to Everness*, *Palworld*, etc.).
                - Correctly binds to the game's actual rendering queue to avoid crashes and queue mismatch errors.
                - Adds queue safety guards to skip HIP calls gracefully when unexpected command lists are dispatched.
                - Flushes the GPU prior to queue migration to reduce VRAM leak risks.
                - Fixes command list split eligibility, removes startup hash delays, and adds log rate-limiting.
              - **Runtime and Resolution Improvements:** Improves exposure handling, buffer resizing, clear handling, and recovery; adds RGB9E5 color input support and improves input admission for wide resolutions up to 2560x1080.
              - **In-Game Controls:** The Ins menu includes real-time `Detail strength` and `Colour strength` sliders, plus live `Debug view` channel inspection.
            - **Dual-Backend Switching:** Supports both `lmxxf` and `danielblnc`. Select a backend in the in-game Ins menu or in `OptiScaler.ini` with `NrBackend=lmxxf` or `NrBackend=daniel`. The first switch from `daniel` to `lmxxf` requires a game restart if the required hooks were not enabled at startup. After a live switch, the previous backend may retain some VRAM until the game exits.
            - **User-Supplied Weights:** Model weights must be provided manually (`native-game-tiled-assets/` for lmxxf, or `dlssnr_on_amd_weights.bin` for danielblnc).
            - **Installer Update:** Fixed installer interaction logic. Stale shader cleanup is limited to retired `native_` / `preblock_` shaders; uninstall preserves unrelated user flags.

            ### Known issues
            - **`lmxxf` Pre-SR above ~1080p internal resolution is not fully integrated yet.** Same-frame neural rendering with FitLarge / larger Color can hitch badly. Prefer internal render at or below roughly: **4K Performance**, **1440p Balanced**, or **1080p native** (keep Pre-SR Color near 1080p). `LmxxfFitLarge` defaults to **false**; only enable explicitly if you accept the cost.

            ### 中文
            - **新增 `lmxxf` HIP 神经渲染后端：** 在现有 `danielblnc` 后端之外加入开源 `lmxxf` 后端，并改进其 DLSS/XeSS 代理接入能力。
              - **修复 lmxxf 后端对虚幻引擎 5（UE5）的兼容性：** 解决复杂 UE5 游戏中的会话失效与回退问题，已在《异环》《幻兽帕鲁》等游戏中测试。
                - 正确绑定游戏实际使用的渲染队列，避免崩溃和队列不匹配错误。
                - 增加队列安全保护；遇到意外提交的命令列表时跳过 HIP 调用。
                - 队列迁移前等待 GPU 完成工作，降低显存泄漏风险。
                - 修复命令列表切分准入判定，移除启动时的哈希延迟，并增加日志限流。
              - **改进 lmxxf 运行时与分辨率支持：** 改进曝光处理、缓冲区尺寸调整、清屏和异常恢复逻辑；增加对 RGB9E5 颜色输入的支持，并改进对宽分辨率输入的接纳，最高至 2560x1080。
              - **游戏内控制：** Ins 菜单提供实时调节 `Detail strength` 和 `Colour strength` 的滑块，以及实时的 `Debug view` 通道检查。
            - **双后端切换：** 同时支持 `lmxxf` 与 `danielblnc`。可在游戏内 Ins 菜单或 `OptiScaler.ini` 中通过 `NrBackend=lmxxf` 或 `NrBackend=daniel` 选择后端。如果启动时未启用所需 hooks，从 `daniel` 首次切换到 `lmxxf` 需要重启游戏。实时切换后，原后端可能会占用部分显存直到游戏退出。
            - **用户仍需自备权重：** 用户需自行提供模型权重（lmxxf 使用 `native-game-tiled-assets/`，danielblnc 使用 `dlssnr_on_amd_weights.bin`）。
            - **安装器与卸载器更新：** 修复安装器交互逻辑及相关行为。过期着色器清理范围限制为已废弃的 `native_` / `preblock_` 着色器；卸载时保留其他用户 flags。

            ### 已知问题
            - **`lmxxf` 超分前（Pre-SR）在约 1080p 以上内部分辨率的神经渲染尚未完成接入。** 开启 FitLarge / 更大 Color 的同帧路径可能严重卡顿。建议内部分辨率大致不超过：**4K 性能档**、**2K 平衡档**、或 **1080p 原生**（尽量让 Pre-SR Color 接近 1080p）。`LmxxfFitLarge` 默认 **false**，仅在接受开销时显式开启。

            - No NVIDIA proprietary binaries, danielblnc setup, or unauthorized model weights in this zip.
            - See README inside the zip.
