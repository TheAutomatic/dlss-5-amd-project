# 第三方运行时 A 的二进制副本与派生镜像 —— 仅作本地分析/部署用，不随项目分发
version*.dll
dlssnr_on_amd_setup.exe

# 神经网络运行时与权重（体积大，且不属于本项目）
nvngx_dlssnr.dll
dlssnr_on_amd_weights.bin

# 本地 mod 试验目录（XeFG 等第三方文件，不随仓库）
/mod/



# 游戏 log 快照与 R1 本地部署包（体积大，只分析用）
exports/onimusha-*/
exports/review-r1/
exports/presentmon-*/

# 打包输出（含 FSR/XeSS/pass DLL，体积大）
OptiScaler-AMD-PreSR-Multipass-v2.25/
OptiScaler-AMD-PreSR-Multipass-v2.25.zip
OptiScaler-AMD-PreSR-R1/
OptiScaler-AMD-PreSR-R1.zip
OptiScaler-AMD-PreSR*.zip

# 本机构建产物
**/x64/Release/
**/x64/Debug/
**/Release/
*.pdb
*.ilk
*.exp


# iMMERSE（Marty's Mods / Pascal Gilcher）RTGI 的本地移植素材 —— 只留本地，不进仓库。
# NOTICE.txt 是随附的第三方声明：「Proprietary and confidential」「Unauthorized copying
# of this file, via any medium is strictly prohibited」；同目录 *.hlsl 是它的 native
# adaptation，iMMERSE_*.rgba 是 iMMERSE 贴图的二进制转换。研究用，不随本项目分发。
# 2026-09-15：git rm -r --cached，磁盘保留。发布的包走 package-amd-presr/experimental_lighting
# （shaders/Lighting.hlsl 编译，自述「no translated effect code」），与此无关。

# 取证构建产物（DLL/日志/obj）—— 回滚用，只留本地，不进仓库
exports/review-s*/

# 本地构建输出（构建脚本 + 打包器用的目录），不进仓库
exports/build/

# tools/build-release-local.cmd 的输出（OptiScaler.dll、obj、测试 exe、build.log）。
# *.dll/*.obj/*.lib 已被下面的全局规则盖住，但 build.log 和 amd_submission_state.exe
# 没有，`git add -A` 会扫进来。
exports/release-local/

/tools/minhook-master
/tools/reshade-main

# 旧的按版本命名的发布目录（r17/r18 等），本地留档用
exports/release-r*/

# Release packaging output (local / CI artifact)
dist/

# 二进制：默认全忽略，只放行必须进仓库的那几个。
# 这条与上游 OptiScaler 一致（它的 .gitignore 也是 *.dll + 单条例外）。
# 注意：gitignore 对「已跟踪」的文件无效，已跟踪的要用 git rm --cached 才生效。
*.dll
!OptiScaler-DLSSNR-PreSR-Multipass-main/external/directx_agility_sdk/lib/D3D12Core.dll

# 编辑器/会话
.claude/
.vs/
.vscode/
*.code-workspace


# Python 字节码缓存
__pycache__/
*.pyc

exports/temporal-a*.log
exports/review-s5/
exports/review-s6/
exports/review-s7/
exports/review-s7-ks1/

# Codex 会话导出与答复汇总 —— 外部代理的中间产物，随时重生成。
# 2026-09-15：`git add -A` 曾把其中两个（`_codex_134944_last/prev.txt`）
# 扫进 777dfcd；靠显式 `git add <文件>` 躲开不是办法，规则挡掉。
exports/_codex_*

# MSVC 中间产物 —— 可由 build-review-*.cmd 完全重建，不应进仓库。
# 2026-09-14：此前 `git add -A exports` 把 review-s*/obj/ 一路扫进来，
# 单个 OptiScaler.pch 达 183 MB，直接导致 GitHub push 被 GH001 拒绝。
**/obj/
*.pch
*.iobj
*.ipdb
*.obj
*.lib



# analysis/ 整个目录只留本地（2026-09-15 定）。
# 原来这里有 30 条逐文件规则；一条整目录盖掉。目录里仍留着本地的
# 二进制分析输入、分析工具链、测试包输出和一次性脚本。
analysis/

# vendored 的二进制分析工具（capstone / pefile / clang-format 副本），只留本地。
.analysis-tools/

# ---------------------------------------------------------------------------
# 内部文档不进仓库（2026-09-15 定）：exports/ 与 .handoff/ 整个忽略，
# GIT-REWRITE-STATUS.md 单条忽略。磁盘上都保留，只是不跟踪、不会随分支推上去。
#
# 起因：`git add -A` / `git add exports/` 已经三次把只该留本地的文件扫进提交。
# 为什么整类收掉：这条血缘链上（OptiScaler / Dagherbou / wilsjo2 / Matheus 四个仓库
# 逐一查过）公开的都是「结论性文档」——版本说明、用户向说明、事后的评审记录、设计稿。
# 没人公开：写给外部审阅者的提问稿、AI 协作痕迹、本机路径，
# 也没有人把第三方闭源产物的去向写进仓库。本目录里的东西属于前者。
#
# 文档以后公开多少，另行决定——先把「不会被误推」这件事锁住。
#
# 注意：gitignore 对**已跟踪**的文件无效。这三个路径已用 `git rm --cached`
# 取消跟踪，规则才真正生效。`.githooks/pre-commit` 拦住 `git add -f` 的绕过。
exports/
.handoff/
GIT-REWRITE-STATUS.md
# D3D shader cache. Regenerated beside the hlsl on first use. Not shipped.
third_party/lmxxf/shaders/shader-cache/


# lmxxf local module build output from sync-lmxxf-upstream.ps1
third_party/lmxxf/hip/_build_gfx1201/

# local HIP module compiler built from rtc_compile.cpp
third_party/lmxxf/hip/rtc_compile.exe
third_party/lmxxf/hip/*.hsaco.s
