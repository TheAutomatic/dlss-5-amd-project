# 第三方运行时 A 的二进制副本与派生镜像 —— 仅作本地分析/部署用，不随项目分发
analysis/setup-*.bin
version*.dll
dlssnr_on_amd_setup.exe

# 神经网络运行时与权重（体积大，且不属于本项目）
nvngx_dlssnr.dll
dlssnr_on_amd_weights.bin

# 官方 OptiScaler 稀疏克隆（只为补 library/*.lib）
analysis/_optiscaler_upstream/

# 本机构建/分析日志（只留本地）
analysis/build-amd.log
analysis/_*.log
analysis/*.log
analysis/_build_amd.bat

# 游戏 log 快照与 R1 本地部署包（体积大，只分析用）
exports/onimusha-*/
exports/review-r1/

# 打包输出（含 FSR/XeSS/pass DLL，体积大）
OptiScaler-AMD-PreSR-Multipass-v2.25/
OptiScaler-AMD-PreSR-Multipass-v2.25.zip

# 编辑器/会话
.claude/
.vs/

# Python 字节码缓存
__pycache__/
*.pyc
